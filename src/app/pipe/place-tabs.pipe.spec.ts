import { PlaceTabsPipe } from './place-tabs.pipe';

describe('PlaceTabsPipe', () => {
  it('create an instance', () => {
    const pipe = new PlaceTabsPipe();
    expect(pipe).toBeTruthy();
  });
});
